import {
  require_dist
} from "./chunk-GOV534AR.js";
import "./chunk-JXZJTKA5.js";
import {
  require_jsx_runtime
} from "./chunk-TTA4GNPI.js";
import {
  __commonJS,
  require_react
} from "./chunk-TRALEDFT.js";

// node_modules/@remotion/player/dist/calculate-scale.js
var require_calculate_scale = __commonJS({
  "node_modules/@remotion/player/dist/calculate-scale.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.calculateScale = void 0;
    var calculateScale = ({ previewSize, compositionWidth, compositionHeight, canvasSize }) => {
      const heightRatio = canvasSize.height / compositionHeight;
      const widthRatio = canvasSize.width / compositionWidth;
      const ratio = Math.min(heightRatio, widthRatio);
      const scale = previewSize === "auto" ? ratio : Number(previewSize);
      const correction = 0 - (1 - scale) / 2;
      const xCorrection = correction * compositionWidth;
      const yCorrection = correction * compositionHeight;
      const width = compositionWidth * scale;
      const height = compositionHeight * scale;
      const centerX = canvasSize.width / 2 - width / 2;
      const centerY = canvasSize.height / 2 - height / 2;
      return {
        centerX,
        centerY,
        xCorrection,
        yCorrection,
        scale
      };
    };
    exports.calculateScale = calculateScale;
  }
});

// node_modules/@remotion/player/dist/emitter-context.js
var require_emitter_context = __commonJS({
  "node_modules/@remotion/player/dist/emitter-context.js"(exports) {
    "use strict";
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PlayerEventEmitterContext = void 0;
    var react_1 = __importDefault(require_react());
    exports.PlayerEventEmitterContext = react_1.default.createContext(void 0);
  }
});

// node_modules/@remotion/player/dist/event-emitter.js
var require_event_emitter = __commonJS({
  "node_modules/@remotion/player/dist/event-emitter.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PlayerEmitter = void 0;
    var PlayerEmitter = class {
      constructor() {
        this.listeners = {
          ended: [],
          error: [],
          pause: [],
          play: [],
          ratechange: [],
          seeked: [],
          timeupdate: [],
          fullscreenchange: []
        };
      }
      addEventListener(name, callback) {
        this.listeners[name].push(callback);
      }
      removeEventListener(name, callback) {
        this.listeners[name] = this.listeners[name].filter((l) => l !== callback);
      }
      dispatchEvent(dispatchName, context) {
        this.listeners[dispatchName].forEach((callback) => {
          callback({ detail: context });
        });
      }
      dispatchSeek(frame) {
        this.dispatchEvent("seeked", {
          frame
        });
      }
      dispatchPause() {
        this.dispatchEvent("pause", void 0);
      }
      dispatchPlay() {
        this.dispatchEvent("play", void 0);
      }
      dispatchEnded() {
        this.dispatchEvent("ended", void 0);
      }
      dispatchRatechange(playbackRate) {
        this.dispatchEvent("ratechange", {
          playbackRate
        });
      }
      dispatchError(error) {
        this.dispatchEvent("error", {
          error
        });
      }
      dispatchTimeUpdate(event) {
        this.dispatchEvent("timeupdate", event);
      }
      dispatchFullscreenChangeUpdate(event) {
        this.dispatchEvent("fullscreenchange", event);
      }
    };
    exports.PlayerEmitter = PlayerEmitter;
  }
});

// node_modules/@remotion/player/dist/use-hover-state.js
var require_use_hover_state = __commonJS({
  "node_modules/@remotion/player/dist/use-hover-state.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useHoverState = void 0;
    var react_1 = require_react();
    var useHoverState = (ref) => {
      const [hovered, stetHovered] = (0, react_1.useState)(false);
      (0, react_1.useEffect)(() => {
        const { current } = ref;
        if (!current) {
          return;
        }
        const onHover = () => {
          stetHovered(true);
        };
        const onLeave = () => {
          stetHovered(false);
        };
        current.addEventListener("mouseenter", onHover);
        current.addEventListener("mouseleave", onLeave);
        return () => {
          current.removeEventListener("mouseenter", onHover);
          current.removeEventListener("mouseenter", onLeave);
        };
      }, [ref]);
      return hovered;
    };
    exports.useHoverState = useHoverState;
  }
});

// node_modules/@remotion/player/dist/calculate-next-frame.js
var require_calculate_next_frame = __commonJS({
  "node_modules/@remotion/player/dist/calculate-next-frame.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.calculateNextFrame = void 0;
    var calculateNextFrame = ({ time, currentFrame: startFrame, playbackSpeed, fps, actualLastFrame, actualFirstFrame, framesAdvanced, shouldLoop }) => {
      const op = playbackSpeed < 0 ? Math.ceil : Math.floor;
      const framesToAdvance = op(time * playbackSpeed / (1e3 / fps)) - framesAdvanced;
      const nextFrame = framesToAdvance + startFrame;
      const isCurrentFrameOutside = startFrame > actualLastFrame || startFrame < actualFirstFrame;
      const isNextFrameOutside = nextFrame > actualLastFrame || nextFrame < actualFirstFrame;
      const hasEnded = !shouldLoop && isNextFrameOutside && !isCurrentFrameOutside;
      if (playbackSpeed > 0) {
        if (isNextFrameOutside) {
          return {
            nextFrame: actualFirstFrame,
            framesToAdvance,
            hasEnded
          };
        }
        return { nextFrame, framesToAdvance, hasEnded };
      }
      if (isNextFrameOutside) {
        return { nextFrame: actualLastFrame, framesToAdvance, hasEnded };
      }
      return { nextFrame, framesToAdvance, hasEnded };
    };
    exports.calculateNextFrame = calculateNextFrame;
  }
});

// node_modules/@remotion/player/dist/use-player.js
var require_use_player = __commonJS({
  "node_modules/@remotion/player/dist/use-player.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.usePlayer = void 0;
    var react_1 = require_react();
    var remotion_1 = require_dist();
    var emitter_context_1 = require_emitter_context();
    var usePlayer = () => {
      var _a;
      const [playing, setPlaying, imperativePlaying] = remotion_1.Internals.Timeline.usePlayingState();
      const frame = remotion_1.Internals.Timeline.useTimelinePosition();
      const playStart = (0, react_1.useRef)(0);
      const setFrame = remotion_1.Internals.Timeline.useTimelineSetFrame();
      const setTimelinePosition = remotion_1.Internals.Timeline.useTimelineSetFrame();
      const audioContext = (0, react_1.useContext)(remotion_1.Internals.SharedAudioContext);
      const { audioAndVideoTags } = (0, react_1.useContext)(remotion_1.Internals.Timeline.TimelineContext);
      const frameRef = (0, react_1.useRef)();
      frameRef.current = frame;
      const video = remotion_1.Internals.useVideo();
      const config = remotion_1.Internals.useUnsafeVideoConfig();
      const emitter = (0, react_1.useContext)(emitter_context_1.PlayerEventEmitterContext);
      const lastFrame = ((_a = config === null || config === void 0 ? void 0 : config.durationInFrames) !== null && _a !== void 0 ? _a : 1) - 1;
      const isLastFrame = frame === lastFrame;
      const isFirstFrame = frame === 0;
      if (!emitter) {
        throw new TypeError("Expected Player event emitter context");
      }
      const seek = (0, react_1.useCallback)((newFrame) => {
        setTimelinePosition(newFrame);
        emitter.dispatchSeek(newFrame);
      }, [emitter, setTimelinePosition]);
      const play = (0, react_1.useCallback)((e) => {
        if (imperativePlaying.current) {
          return;
        }
        if (isLastFrame) {
          seek(0);
        }
        if (audioContext && audioContext.numberOfAudioTags > 0 && e) {
          audioContext.playAllAudios();
        }
        audioAndVideoTags.current.forEach((a) => a.play());
        imperativePlaying.current = true;
        setPlaying(true);
        playStart.current = frameRef.current;
        emitter.dispatchPlay();
      }, [
        imperativePlaying,
        isLastFrame,
        audioContext,
        setPlaying,
        emitter,
        seek,
        audioAndVideoTags
      ]);
      const pause = (0, react_1.useCallback)(() => {
        if (imperativePlaying.current) {
          imperativePlaying.current = false;
          setPlaying(false);
          emitter.dispatchPause();
        }
      }, [emitter, imperativePlaying, setPlaying]);
      const pauseAndReturnToPlayStart = (0, react_1.useCallback)(() => {
        if (imperativePlaying.current) {
          imperativePlaying.current = false;
          setTimelinePosition(playStart.current);
          setPlaying(false);
          emitter.dispatchPause();
        }
      }, [emitter, imperativePlaying, setPlaying, setTimelinePosition]);
      const hasVideo = Boolean(video);
      const frameBack = (0, react_1.useCallback)((frames) => {
        if (!hasVideo) {
          return null;
        }
        if (imperativePlaying.current) {
          return;
        }
        setFrame((f) => {
          return Math.max(0, f - frames);
        });
      }, [hasVideo, imperativePlaying, setFrame]);
      const frameForward = (0, react_1.useCallback)((frames) => {
        if (!hasVideo) {
          return null;
        }
        if (imperativePlaying.current) {
          return;
        }
        setFrame((f) => Math.min(lastFrame, f + frames));
      }, [hasVideo, imperativePlaying, lastFrame, setFrame]);
      const returnValue = (0, react_1.useMemo)(() => {
        return {
          frameBack,
          frameForward,
          isLastFrame,
          emitter,
          playing,
          play,
          pause,
          seek,
          isFirstFrame,
          getCurrentFrame: () => frameRef.current,
          isPlaying: () => imperativePlaying.current,
          pauseAndReturnToPlayStart
        };
      }, [
        frameBack,
        frameForward,
        isLastFrame,
        emitter,
        playing,
        play,
        pause,
        seek,
        isFirstFrame,
        pauseAndReturnToPlayStart,
        imperativePlaying
      ]);
      return returnValue;
    };
    exports.usePlayer = usePlayer;
  }
});

// node_modules/@remotion/player/dist/use-playback.js
var require_use_playback = __commonJS({
  "node_modules/@remotion/player/dist/use-playback.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.usePlayback = void 0;
    var react_1 = require_react();
    var remotion_1 = require_dist();
    var calculate_next_frame_1 = require_calculate_next_frame();
    var use_player_1 = require_use_player();
    var usePlayback = ({ loop, playbackRate, moveToBeginningWhenEnded, inFrame, outFrame }) => {
      const frame = remotion_1.Internals.Timeline.useTimelinePosition();
      const config = remotion_1.Internals.useUnsafeVideoConfig();
      const { playing, pause, emitter } = (0, use_player_1.usePlayer)();
      const setFrame = remotion_1.Internals.Timeline.useTimelineSetFrame();
      const frameRef = (0, react_1.useRef)(frame);
      frameRef.current = frame;
      const lastTimeUpdateEvent = (0, react_1.useRef)(null);
      (0, react_1.useEffect)(() => {
        if (!config) {
          return;
        }
        if (!playing) {
          return;
        }
        let hasBeenStopped = false;
        let reqAnimFrameCall = null;
        const startedTime = performance.now();
        let framesAdvanced = 0;
        const stop = () => {
          hasBeenStopped = true;
          if (reqAnimFrameCall !== null) {
            cancelAnimationFrame(reqAnimFrameCall);
          }
        };
        const callback = () => {
          const time = performance.now() - startedTime;
          const actualLastFrame = outFrame !== null && outFrame !== void 0 ? outFrame : config.durationInFrames - 1;
          const actualFirstFrame = inFrame !== null && inFrame !== void 0 ? inFrame : 0;
          const { nextFrame, framesToAdvance, hasEnded } = (0, calculate_next_frame_1.calculateNextFrame)({
            time,
            currentFrame: frameRef.current,
            playbackSpeed: playbackRate,
            fps: config.fps,
            actualFirstFrame,
            actualLastFrame,
            framesAdvanced,
            shouldLoop: loop
          });
          framesAdvanced += framesToAdvance;
          if (nextFrame !== frameRef.current && (!hasEnded || moveToBeginningWhenEnded)) {
            setFrame(nextFrame);
          }
          if (hasEnded) {
            stop();
            pause();
            emitter.dispatchEnded();
            return;
          }
          if (!hasBeenStopped) {
            reqAnimFrameCall = requestAnimationFrame(callback);
          }
        };
        reqAnimFrameCall = requestAnimationFrame(callback);
        return () => {
          stop();
        };
      }, [
        config,
        loop,
        pause,
        playing,
        setFrame,
        emitter,
        playbackRate,
        inFrame,
        outFrame,
        moveToBeginningWhenEnded
      ]);
      (0, react_1.useEffect)(() => {
        const interval = setInterval(() => {
          if (lastTimeUpdateEvent.current === frameRef.current) {
            return;
          }
          emitter.dispatchTimeUpdate({ frame: frameRef.current });
          lastTimeUpdateEvent.current = frameRef.current;
        }, 250);
        return () => clearInterval(interval);
      }, [emitter]);
    };
    exports.usePlayback = usePlayback;
  }
});

// node_modules/@remotion/player/dist/utils/use-element-size.js
var require_use_element_size = __commonJS({
  "node_modules/@remotion/player/dist/utils/use-element-size.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useElementSize = exports.updateAllElementsSizes = void 0;
    var react_1 = require_react();
    var elementSizeHooks = [];
    var updateAllElementsSizes = () => {
      for (const listener of elementSizeHooks) {
        listener();
      }
    };
    exports.updateAllElementsSizes = updateAllElementsSizes;
    var useElementSize = (ref, options) => {
      const [size, setSize] = (0, react_1.useState)(null);
      const observer = (0, react_1.useMemo)(() => {
        if (typeof ResizeObserver === "undefined") {
          return null;
        }
        return new ResizeObserver((entries) => {
          const { contentRect } = entries[0];
          const newSize = entries[0].target.getClientRects();
          if (!newSize || !newSize[0]) {
            setSize(null);
            return;
          }
          const probableCssParentScale = newSize[0].width / contentRect.width;
          const width = options.shouldApplyCssTransforms ? newSize[0].width : newSize[0].width * (1 / probableCssParentScale);
          const height = options.shouldApplyCssTransforms ? newSize[0].height : newSize[0].height * (1 / probableCssParentScale);
          setSize({
            width,
            height,
            left: newSize[0].x,
            top: newSize[0].y,
            windowSize: {
              height: window.innerHeight,
              width: window.innerWidth
            }
          });
        });
      }, [options.shouldApplyCssTransforms]);
      const updateSize = (0, react_1.useCallback)(() => {
        if (!ref.current) {
          return;
        }
        const rect = ref.current.getClientRects();
        if (!rect[0]) {
          setSize(null);
          return;
        }
        setSize({
          width: rect[0].width,
          height: rect[0].height,
          left: rect[0].x,
          top: rect[0].y,
          windowSize: {
            height: window.innerHeight,
            width: window.innerWidth
          }
        });
      }, [ref]);
      (0, react_1.useEffect)(() => {
        if (!observer) {
          return;
        }
        updateSize();
        const { current } = ref;
        if (ref.current) {
          observer.observe(ref.current);
        }
        return () => {
          if (current) {
            observer.unobserve(current);
          }
        };
      }, [observer, ref, updateSize]);
      (0, react_1.useEffect)(() => {
        if (!options.triggerOnWindowResize) {
          return;
        }
        window.addEventListener("resize", updateSize);
        return () => {
          window.removeEventListener("resize", updateSize);
        };
      }, [options.triggerOnWindowResize, updateSize]);
      (0, react_1.useEffect)(() => {
        elementSizeHooks.push(updateSize);
        return () => {
          elementSizeHooks = elementSizeHooks.filter((e) => e !== updateSize);
        };
      }, [updateSize]);
      return size;
    };
    exports.useElementSize = useElementSize;
  }
});

// node_modules/@remotion/player/dist/player-css-classname.js
var require_player_css_classname = __commonJS({
  "node_modules/@remotion/player/dist/player-css-classname.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.VOLUME_SLIDER_INPUT_CSS_CLASSNAME = exports.PLAYER_CSS_CLASSNAME = void 0;
    exports.PLAYER_CSS_CLASSNAME = "__remotion-player";
    exports.VOLUME_SLIDER_INPUT_CSS_CLASSNAME = exports.PLAYER_CSS_CLASSNAME.concat("_volume-slider-input");
  }
});

// node_modules/@remotion/player/dist/error-boundary.js
var require_error_boundary = __commonJS({
  "node_modules/@remotion/player/dist/error-boundary.js"(exports) {
    "use strict";
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ErrorBoundary = void 0;
    var jsx_runtime_1 = require_jsx_runtime();
    var react_1 = __importDefault(require_react());
    var errorStyle = {
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      flex: 1,
      height: "100%",
      width: "100%"
    };
    var ErrorBoundary = class extends react_1.default.Component {
      constructor() {
        super(...arguments);
        this.state = { hasError: null };
      }
      static getDerivedStateFromError(error) {
        return { hasError: error };
      }
      componentDidCatch(error) {
        this.props.onError(error);
      }
      render() {
        if (this.state.hasError) {
          return (0, jsx_runtime_1.jsx)("div", { style: errorStyle, children: this.props.errorFallback({
            error: this.state.hasError
          }) });
        }
        return this.props.children;
      }
    };
    exports.ErrorBoundary = ErrorBoundary;
  }
});

// node_modules/@remotion/player/dist/format-time.js
var require_format_time = __commonJS({
  "node_modules/@remotion/player/dist/format-time.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.formatTime = void 0;
    var formatTime = (timeInSeconds) => {
      const minutes = Math.floor(timeInSeconds / 60);
      const seconds = Math.floor(timeInSeconds - minutes * 60);
      return `${String(minutes)}:${String(seconds).padStart(2, "0")}`;
    };
    exports.formatTime = formatTime;
  }
});

// node_modules/@remotion/player/dist/icons.js
var require_icons = __commonJS({
  "node_modules/@remotion/player/dist/icons.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.VolumeOnIcon = exports.VolumeOffIcon = exports.FullscreenIcon = exports.PauseIcon = exports.PlayIcon = exports.ICON_SIZE = void 0;
    var jsx_runtime_1 = require_jsx_runtime();
    exports.ICON_SIZE = 25;
    var fullscreenIconSize = 16;
    var rotate = {
      transform: `rotate(90deg)`
    };
    var PlayIcon = () => {
      return (0, jsx_runtime_1.jsx)("svg", { width: exports.ICON_SIZE, height: exports.ICON_SIZE, viewBox: "-100 -100 400 400", style: rotate, children: (0, jsx_runtime_1.jsx)("path", { fill: "#fff", stroke: "#fff", strokeWidth: "100", strokeLinejoin: "round", d: "M 2 172 a 196 100 0 0 0 195 5 A 196 240 0 0 0 100 2.259 A 196 240 0 0 0 2 172 z" }) });
    };
    exports.PlayIcon = PlayIcon;
    var PauseIcon = () => {
      return (0, jsx_runtime_1.jsxs)("svg", { viewBox: "0 0 100 100", width: exports.ICON_SIZE, height: exports.ICON_SIZE, children: [(0, jsx_runtime_1.jsx)("rect", { x: "25", y: "20", width: "20", height: "60", fill: "#fff", ry: "5", rx: "5" }), (0, jsx_runtime_1.jsx)("rect", { x: "55", y: "20", width: "20", height: "60", fill: "#fff", ry: "5", rx: "5" })] });
    };
    exports.PauseIcon = PauseIcon;
    var FullscreenIcon = ({ minimized }) => {
      const strokeWidth = 6;
      const viewSize = 32;
      const out = minimized ? strokeWidth / 2 : 0;
      const middleInset = minimized ? strokeWidth / 2 : strokeWidth * 1.6;
      const inset = minimized ? strokeWidth * 2 : strokeWidth * 1.6;
      return (0, jsx_runtime_1.jsxs)("svg", { viewBox: `0 0 ${viewSize} ${viewSize}`, height: fullscreenIconSize, width: fullscreenIconSize, children: [(0, jsx_runtime_1.jsx)("path", { d: `
				M ${out} ${inset}
				L ${middleInset} ${middleInset}
				L ${inset} ${out}
				`, stroke: "#fff", strokeWidth, fill: "none" }), (0, jsx_runtime_1.jsx)("path", { d: `
				M ${viewSize - out} ${inset}
				L ${viewSize - middleInset} ${middleInset}
				L ${viewSize - inset} ${out}
				`, stroke: "#fff", strokeWidth, fill: "none" }), (0, jsx_runtime_1.jsx)("path", { d: `
				M ${out} ${viewSize - inset}
				L ${middleInset} ${viewSize - middleInset}
				L ${inset} ${viewSize - out}
				`, stroke: "#fff", strokeWidth, fill: "none" }), (0, jsx_runtime_1.jsx)("path", { d: `
				M ${viewSize - out} ${viewSize - inset}
				L ${viewSize - middleInset} ${viewSize - middleInset}
				L ${viewSize - inset} ${viewSize - out}
				`, stroke: "#fff", strokeWidth, fill: "none" })] });
    };
    exports.FullscreenIcon = FullscreenIcon;
    var VolumeOffIcon = () => {
      return (0, jsx_runtime_1.jsx)("svg", { width: exports.ICON_SIZE, height: exports.ICON_SIZE, viewBox: "0 0 24 24", children: (0, jsx_runtime_1.jsx)("path", { d: "M3.63 3.63a.996.996 0 000 1.41L7.29 8.7 7 9H4c-.55 0-1 .45-1 1v4c0 .55.45 1 1 1h3l3.29 3.29c.63.63 1.71.18 1.71-.71v-4.17l4.18 4.18c-.49.37-1.02.68-1.6.91-.36.15-.58.53-.58.92 0 .72.73 1.18 1.39.91.8-.33 1.55-.77 2.22-1.31l1.34 1.34a.996.996 0 101.41-1.41L5.05 3.63c-.39-.39-1.02-.39-1.42 0zM19 12c0 .82-.15 1.61-.41 2.34l1.53 1.53c.56-1.17.88-2.48.88-3.87 0-3.83-2.4-7.11-5.78-8.4-.59-.23-1.22.23-1.22.86v.19c0 .38.25.71.61.85C17.18 6.54 19 9.06 19 12zm-8.71-6.29l-.17.17L12 7.76V6.41c0-.89-1.08-1.33-1.71-.7zM16.5 12A4.5 4.5 0 0014 7.97v1.79l2.48 2.48c.01-.08.02-.16.02-.24z", fill: "#fff" }) });
    };
    exports.VolumeOffIcon = VolumeOffIcon;
    var VolumeOnIcon = () => {
      return (0, jsx_runtime_1.jsx)("svg", { width: exports.ICON_SIZE, height: exports.ICON_SIZE, viewBox: "0 0 24 24", children: (0, jsx_runtime_1.jsx)("path", { d: "M3 10v4c0 .55.45 1 1 1h3l3.29 3.29c.63.63 1.71.18 1.71-.71V6.41c0-.89-1.08-1.34-1.71-.71L7 9H4c-.55 0-1 .45-1 1zm13.5 2A4.5 4.5 0 0014 7.97v8.05c1.48-.73 2.5-2.25 2.5-4.02zM14 4.45v.2c0 .38.25.71.6.85C17.18 6.53 19 9.06 19 12s-1.82 5.47-4.4 6.5c-.36.14-.6.47-.6.85v.2c0 .63.63 1.07 1.21.85C18.6 19.11 21 15.84 21 12s-2.4-7.11-5.79-8.4c-.58-.23-1.21.22-1.21.85z", fill: "#fff" }) });
    };
    exports.VolumeOnIcon = VolumeOnIcon;
  }
});

// node_modules/@remotion/player/dist/MediaVolumeSlider.js
var require_MediaVolumeSlider = __commonJS({
  "node_modules/@remotion/player/dist/MediaVolumeSlider.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MediaVolumeSlider = void 0;
    var jsx_runtime_1 = require_jsx_runtime();
    var react_1 = require_react();
    var remotion_1 = require_dist();
    var icons_1 = require_icons();
    var player_css_classname_1 = require_player_css_classname();
    var use_hover_state_1 = require_use_hover_state();
    var BAR_HEIGHT = 5;
    var KNOB_SIZE = 12;
    var VOLUME_SLIDER_WIDTH = 100;
    var scope = `.${player_css_classname_1.VOLUME_SLIDER_INPUT_CSS_CLASSNAME}`;
    var sliderStyle = `
	${scope} {
		-webkit-appearance: none;
		background-color: rgba(255, 255, 255, 0.5);	
		border-radius: ${BAR_HEIGHT / 2}px;
		cursor: pointer;
		height: ${BAR_HEIGHT}px;
		margin-left: 5px;
		width: ${VOLUME_SLIDER_WIDTH}px;
	}

	${scope}::-webkit-slider-thumb {
		-webkit-appearance: none;
		background-color: white;
		border-radius: ${KNOB_SIZE / 2}px;
		box-shadow: 0 0 2px black;
		height: ${KNOB_SIZE}px;
		width: ${KNOB_SIZE}px;
	}

	${scope}::-moz-range-thumb {
		-webkit-appearance: none;
		background-color: white;
		border-radius: ${KNOB_SIZE / 2}px;
		box-shadow: 0 0 2px black;
		height: ${KNOB_SIZE}px;
		width: ${KNOB_SIZE}px;
	}
`;
    remotion_1.Internals.CSSUtils.injectCSS(sliderStyle);
    var parentDivStyle = {
      display: "inline-flex",
      background: "none",
      border: "none",
      padding: "6px",
      justifyContent: "center",
      alignItems: "center",
      touchAction: "none"
    };
    var volumeContainer = {
      display: "inline",
      width: icons_1.ICON_SIZE,
      height: icons_1.ICON_SIZE,
      cursor: "pointer",
      appearance: "none",
      background: "none",
      border: "none",
      padding: 0
    };
    var MediaVolumeSlider = () => {
      const [mediaMuted, setMediaMuted] = remotion_1.Internals.useMediaMutedState();
      const [mediaVolume, setMediaVolume] = remotion_1.Internals.useMediaVolumeState();
      const [focused, setFocused] = (0, react_1.useState)(false);
      const parentDivRef = (0, react_1.useRef)(null);
      const inputRef = (0, react_1.useRef)(null);
      const hover = (0, use_hover_state_1.useHoverState)(parentDivRef);
      const isMutedOrZero = mediaMuted || mediaVolume === 0;
      const onVolumeChange = (e) => {
        setMediaVolume(parseFloat(e.target.value));
      };
      const onBlur = () => {
        setTimeout(() => {
          if (document.activeElement !== inputRef.current) {
            setFocused(false);
          }
        }, 10);
      };
      const onClick = (0, react_1.useCallback)(() => {
        if (mediaVolume === 0) {
          setMediaVolume(1);
          setMediaMuted(false);
          return;
        }
        setMediaMuted((mute) => !mute);
      }, [mediaVolume, setMediaMuted, setMediaVolume]);
      return (0, jsx_runtime_1.jsxs)("div", { ref: parentDivRef, style: parentDivStyle, children: [(0, jsx_runtime_1.jsx)("button", { "aria-label": isMutedOrZero ? "Unmute sound" : "Mute sound", title: isMutedOrZero ? "Unmute sound" : "Mute sound", onClick, onBlur, onFocus: () => setFocused(true), style: volumeContainer, type: "button", children: isMutedOrZero ? (0, jsx_runtime_1.jsx)(icons_1.VolumeOffIcon, {}) : (0, jsx_runtime_1.jsx)(icons_1.VolumeOnIcon, {}) }), (focused || hover) && !mediaMuted ? (0, jsx_runtime_1.jsx)("input", { ref: inputRef, "aria-label": "Change volume", className: player_css_classname_1.VOLUME_SLIDER_INPUT_CSS_CLASSNAME, max: 1, min: 0, onBlur: () => setFocused(false), onChange: onVolumeChange, step: 0.01, type: "range", value: mediaVolume }) : null] });
    };
    exports.MediaVolumeSlider = MediaVolumeSlider;
  }
});

// node_modules/@remotion/player/dist/PlayerSeekBar.js
var require_PlayerSeekBar = __commonJS({
  "node_modules/@remotion/player/dist/PlayerSeekBar.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PlayerSeekBar = void 0;
    var jsx_runtime_1 = require_jsx_runtime();
    var react_1 = require_react();
    var remotion_1 = require_dist();
    var use_hover_state_1 = require_use_hover_state();
    var use_player_1 = require_use_player();
    var use_element_size_1 = require_use_element_size();
    var getFrameFromX = (clientX, durationInFrames, width) => {
      var _a;
      const pos = clientX;
      const frame = Math.round((0, remotion_1.interpolate)(pos, [0, width], [0, (_a = durationInFrames - 1) !== null && _a !== void 0 ? _a : 0], {
        extrapolateLeft: "clamp",
        extrapolateRight: "clamp"
      }));
      return frame;
    };
    var BAR_HEIGHT = 5;
    var KNOB_SIZE = 12;
    var VERTICAL_PADDING = 4;
    var containerStyle = {
      userSelect: "none",
      paddingTop: VERTICAL_PADDING,
      paddingBottom: VERTICAL_PADDING,
      boxSizing: "border-box",
      cursor: "pointer",
      position: "relative",
      touchAction: "none"
    };
    var barBackground = {
      height: BAR_HEIGHT,
      backgroundColor: "rgba(255, 255, 255, 0.5)",
      width: "100%",
      borderRadius: BAR_HEIGHT / 2
    };
    var PlayerSeekBar = ({ durationInFrames }) => {
      const containerRef = (0, react_1.useRef)(null);
      const barHovered = (0, use_hover_state_1.useHoverState)(containerRef);
      const size = (0, use_element_size_1.useElementSize)(containerRef, {
        triggerOnWindowResize: true,
        shouldApplyCssTransforms: true
      });
      const { seek, play, pause, playing } = (0, use_player_1.usePlayer)();
      const frame = remotion_1.Internals.Timeline.useTimelinePosition();
      const [dragging, setDragging] = (0, react_1.useState)({
        dragging: false
      });
      const onPointerDown = (0, react_1.useCallback)((e) => {
        if (!size) {
          throw new Error("Player has no size");
        }
        const _frame = getFrameFromX(e.clientX - size.left, durationInFrames, size.width);
        pause();
        seek(_frame);
        setDragging({
          dragging: true,
          wasPlaying: playing
        });
      }, [size, durationInFrames, pause, seek, playing]);
      const onPointerMove = (0, react_1.useCallback)((e) => {
        var _a;
        if (!size) {
          throw new Error("Player has no size");
        }
        if (!dragging.dragging) {
          return;
        }
        const _frame = getFrameFromX(e.clientX - ((_a = size === null || size === void 0 ? void 0 : size.left) !== null && _a !== void 0 ? _a : 0), durationInFrames, size.width);
        seek(_frame);
      }, [dragging.dragging, durationInFrames, seek, size]);
      const onPointerUp = (0, react_1.useCallback)(() => {
        setDragging({
          dragging: false
        });
        if (!dragging.dragging) {
          return;
        }
        if (dragging.wasPlaying) {
          play();
        } else {
          pause();
        }
      }, [dragging, pause, play]);
      (0, react_1.useEffect)(() => {
        if (!dragging.dragging) {
          return;
        }
        window.addEventListener("pointermove", onPointerMove);
        window.addEventListener("pointerup", onPointerUp);
        return () => {
          window.removeEventListener("pointermove", onPointerMove);
          window.removeEventListener("pointerup", onPointerUp);
        };
      }, [dragging.dragging, onPointerMove, onPointerUp]);
      const knobStyle = (0, react_1.useMemo)(() => {
        var _a;
        return {
          height: KNOB_SIZE,
          width: KNOB_SIZE,
          borderRadius: KNOB_SIZE / 2,
          position: "absolute",
          top: VERTICAL_PADDING - KNOB_SIZE / 2 + 5 / 2,
          backgroundColor: "white",
          left: Math.max(0, frame / Math.max(1, durationInFrames - 1) * ((_a = size === null || size === void 0 ? void 0 : size.width) !== null && _a !== void 0 ? _a : 0) - KNOB_SIZE / 2),
          boxShadow: "0 0 2px black",
          opacity: Number(barHovered)
        };
      }, [barHovered, durationInFrames, frame, size]);
      const fillStyle = (0, react_1.useMemo)(() => {
        return {
          height: BAR_HEIGHT,
          backgroundColor: "rgba(255, 255, 255, 1)",
          width: frame / (durationInFrames - 1) * 100 + "%",
          borderRadius: BAR_HEIGHT / 2
        };
      }, [durationInFrames, frame]);
      return (0, jsx_runtime_1.jsxs)("div", { ref: containerRef, onPointerDown, style: containerStyle, children: [(0, jsx_runtime_1.jsx)("div", { style: barBackground, children: (0, jsx_runtime_1.jsx)("div", { style: fillStyle }) }), (0, jsx_runtime_1.jsx)("div", { style: knobStyle })] });
    };
    exports.PlayerSeekBar = PlayerSeekBar;
  }
});

// node_modules/@remotion/player/dist/PlayerControls.js
var require_PlayerControls = __commonJS({
  "node_modules/@remotion/player/dist/PlayerControls.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Controls = void 0;
    var jsx_runtime_1 = require_jsx_runtime();
    var react_1 = require_react();
    var remotion_1 = require_dist();
    var format_time_1 = require_format_time();
    var icons_1 = require_icons();
    var MediaVolumeSlider_1 = require_MediaVolumeSlider();
    var PlayerSeekBar_1 = require_PlayerSeekBar();
    var containerStyle = {
      boxSizing: "border-box",
      position: "absolute",
      bottom: 0,
      width: "100%",
      paddingTop: 10,
      paddingBottom: 10,
      background: "linear-gradient(transparent, rgba(0, 0, 0, 0.4))",
      display: "flex",
      paddingRight: 12,
      paddingLeft: 12,
      flexDirection: "column",
      transition: "opacity 0.3s"
    };
    var buttonStyle = {
      appearance: "none",
      backgroundColor: "transparent",
      border: "none",
      cursor: "pointer",
      padding: 0,
      display: "inline",
      marginBottom: 0,
      marginTop: 0,
      height: 25
    };
    var controlsRow = {
      display: "flex",
      flexDirection: "row",
      width: "100%",
      alignItems: "center",
      justifyContent: "center",
      userSelect: "none"
    };
    var leftPartStyle = {
      display: "flex",
      flexDirection: "row",
      userSelect: "none",
      alignItems: "center"
    };
    var xSpacer = {
      width: 10
    };
    var ySpacer = {
      height: 8
    };
    var flex1 = {
      flex: 1
    };
    var fullscreen = {};
    var timeLabel = {
      color: "white",
      fontFamily: "sans-serif",
      fontSize: 14
    };
    var Controls = ({ durationInFrames, hovered, isFullscreen, fps, player, showVolumeControls, onFullscreenButtonClick, allowFullscreen, onExitFullscreenButtonClick, spaceKeyToPlayOrPause }) => {
      const playButtonRef = (0, react_1.useRef)(null);
      const frame = remotion_1.Internals.Timeline.useTimelinePosition();
      const [supportsFullscreen, setSupportsFullscreen] = (0, react_1.useState)(false);
      const containerCss = (0, react_1.useMemo)(() => {
        const shouldShow = hovered || !player.playing;
        return {
          ...containerStyle,
          opacity: Number(shouldShow)
        };
      }, [hovered, player.playing]);
      (0, react_1.useEffect)(() => {
        if (playButtonRef.current && spaceKeyToPlayOrPause) {
          playButtonRef.current.focus({
            preventScroll: true
          });
        }
      }, [player.playing, spaceKeyToPlayOrPause]);
      (0, react_1.useEffect)(() => {
        var _a;
        setSupportsFullscreen((_a = typeof document !== "undefined" && (document.fullscreenEnabled || document.webkitFullscreenEnabled)) !== null && _a !== void 0 ? _a : false);
      }, []);
      return (0, jsx_runtime_1.jsxs)("div", { style: containerCss, children: [(0, jsx_runtime_1.jsxs)("div", { style: controlsRow, children: [(0, jsx_runtime_1.jsxs)("div", { style: leftPartStyle, children: [(0, jsx_runtime_1.jsx)("button", { ref: playButtonRef, type: "button", style: buttonStyle, onClick: player.playing ? player.pause : player.play, "aria-label": player.playing ? "Pause video" : "Play video", title: player.playing ? "Pause video" : "Play video", children: player.playing ? (0, jsx_runtime_1.jsx)(icons_1.PauseIcon, {}) : (0, jsx_runtime_1.jsx)(icons_1.PlayIcon, {}) }), showVolumeControls ? (0, jsx_runtime_1.jsxs)(jsx_runtime_1.Fragment, { children: [(0, jsx_runtime_1.jsx)("div", { style: xSpacer }), (0, jsx_runtime_1.jsx)(MediaVolumeSlider_1.MediaVolumeSlider, {})] }) : null, (0, jsx_runtime_1.jsx)("div", { style: xSpacer }), (0, jsx_runtime_1.jsxs)("div", { style: timeLabel, children: [(0, format_time_1.formatTime)(frame / fps), " / ", (0, format_time_1.formatTime)(durationInFrames / fps)] }), (0, jsx_runtime_1.jsx)("div", { style: xSpacer })] }), (0, jsx_runtime_1.jsx)("div", { style: flex1 }), (0, jsx_runtime_1.jsx)("div", { style: fullscreen, children: supportsFullscreen && allowFullscreen ? (0, jsx_runtime_1.jsx)("button", { type: "button", "aria-label": isFullscreen ? "Exit fullscreen" : "Enter Fullscreen", title: isFullscreen ? "Exit fullscreen" : "Enter Fullscreen", style: buttonStyle, onClick: isFullscreen ? onExitFullscreenButtonClick : onFullscreenButtonClick, children: (0, jsx_runtime_1.jsx)(icons_1.FullscreenIcon, { minimized: !isFullscreen }) }) : null })] }), (0, jsx_runtime_1.jsx)("div", { style: ySpacer }), (0, jsx_runtime_1.jsx)(PlayerSeekBar_1.PlayerSeekBar, { durationInFrames })] });
    };
    exports.Controls = Controls;
  }
});

// node_modules/@remotion/player/dist/utils/calculate-player-size.js
var require_calculate_player_size = __commonJS({
  "node_modules/@remotion/player/dist/utils/calculate-player-size.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.calculatePlayerSize = void 0;
    var calculatePlayerSize = ({ currentSize, width, height, compositionWidth, compositionHeight }) => {
      const aspectRatio = compositionWidth / compositionHeight;
      if (!currentSize) {
        return {
          width: compositionWidth,
          height: compositionHeight
        };
      }
      if (width !== void 0 && height === void 0) {
        return {
          height: currentSize.width / aspectRatio
        };
      }
      if (height !== void 0 && width === void 0) {
        return {
          width: currentSize.height * aspectRatio
        };
      }
      return {
        width: compositionWidth,
        height: compositionHeight
      };
    };
    exports.calculatePlayerSize = calculatePlayerSize;
  }
});

// node_modules/@remotion/player/dist/utils/is-node.js
var require_is_node = __commonJS({
  "node_modules/@remotion/player/dist/utils/is-node.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.IS_NODE = void 0;
    exports.IS_NODE = typeof document === "undefined";
  }
});

// node_modules/@remotion/player/dist/utils/cancellable-promise.js
var require_cancellable_promise = __commonJS({
  "node_modules/@remotion/player/dist/utils/cancellable-promise.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.cancellablePromise = void 0;
    var cancellablePromise = (promise) => {
      let isCanceled = false;
      const wrappedPromise = new Promise((resolve, reject) => {
        promise.then((value) => {
          if (isCanceled) {
            reject({ isCanceled, value });
            return;
          }
          resolve(value);
        }).catch((error) => {
          reject({ isCanceled, error });
        });
      });
      return {
        promise: wrappedPromise,
        cancel: () => {
          isCanceled = true;
        }
      };
    };
    exports.cancellablePromise = cancellablePromise;
  }
});

// node_modules/@remotion/player/dist/utils/delay.js
var require_delay = __commonJS({
  "node_modules/@remotion/player/dist/utils/delay.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.delay = void 0;
    var delay = (n) => new Promise((resolve) => setTimeout(resolve, n));
    exports.delay = delay;
  }
});

// node_modules/@remotion/player/dist/utils/use-cancellable-promises.js
var require_use_cancellable_promises = __commonJS({
  "node_modules/@remotion/player/dist/utils/use-cancellable-promises.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useCancellablePromises = void 0;
    var react_1 = require_react();
    var useCancellablePromises = () => {
      const pendingPromises = (0, react_1.useRef)([]);
      const appendPendingPromise = (0, react_1.useCallback)((promise) => {
        pendingPromises.current = [...pendingPromises.current, promise];
      }, []);
      const removePendingPromise = (0, react_1.useCallback)((promise) => {
        pendingPromises.current = pendingPromises.current.filter((p) => p !== promise);
      }, []);
      const clearPendingPromises = (0, react_1.useCallback)(() => pendingPromises.current.map((p) => p.cancel()), []);
      const api = (0, react_1.useMemo)(() => ({
        appendPendingPromise,
        removePendingPromise,
        clearPendingPromises
      }), [appendPendingPromise, clearPendingPromises, removePendingPromise]);
      return api;
    };
    exports.useCancellablePromises = useCancellablePromises;
  }
});

// node_modules/@remotion/player/dist/utils/use-click-prevention-on-double-click.js
var require_use_click_prevention_on_double_click = __commonJS({
  "node_modules/@remotion/player/dist/utils/use-click-prevention-on-double-click.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useClickPreventionOnDoubleClick = void 0;
    var react_1 = require_react();
    var cancellable_promise_1 = require_cancellable_promise();
    var delay_1 = require_delay();
    var use_cancellable_promises_1 = require_use_cancellable_promises();
    var useClickPreventionOnDoubleClick = (onClick, onDoubleClick, doubleClickToFullscreen) => {
      const api = (0, use_cancellable_promises_1.useCancellablePromises)();
      const handleClick = (0, react_1.useCallback)(async (e) => {
        api.clearPendingPromises();
        const waitForClick = (0, cancellable_promise_1.cancellablePromise)((0, delay_1.delay)(200));
        api.appendPendingPromise(waitForClick);
        try {
          await waitForClick.promise;
          api.removePendingPromise(waitForClick);
          onClick(e);
        } catch (errorInfo) {
          const info = errorInfo;
          api.removePendingPromise(waitForClick);
          if (!info.isCanceled) {
            throw info.error;
          }
        }
      }, [api, onClick]);
      const handleDoubleClick = (0, react_1.useCallback)(() => {
        api.clearPendingPromises();
        onDoubleClick();
      }, [api, onDoubleClick]);
      const returnValue = (0, react_1.useMemo)(() => {
        if (!doubleClickToFullscreen) {
          return [onClick, onDoubleClick];
        }
        return [handleClick, handleDoubleClick];
      }, [
        doubleClickToFullscreen,
        handleClick,
        handleDoubleClick,
        onClick,
        onDoubleClick
      ]);
      return returnValue;
    };
    exports.useClickPreventionOnDoubleClick = useClickPreventionOnDoubleClick;
  }
});

// node_modules/@remotion/player/dist/PlayerUI.js
var require_PlayerUI = __commonJS({
  "node_modules/@remotion/player/dist/PlayerUI.js"(exports) {
    "use strict";
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? function(o, v) {
      Object.defineProperty(o, "default", { enumerable: true, value: v });
    } : function(o, v) {
      o["default"] = v;
    });
    var __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) {
        for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
      }
      __setModuleDefault(result, mod);
      return result;
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    var jsx_runtime_1 = require_jsx_runtime();
    var react_1 = __importStar(require_react());
    var remotion_1 = require_dist();
    var calculate_scale_1 = require_calculate_scale();
    var error_boundary_1 = require_error_boundary();
    var player_css_classname_1 = require_player_css_classname();
    var PlayerControls_1 = require_PlayerControls();
    var use_hover_state_1 = require_use_hover_state();
    var use_playback_1 = require_use_playback();
    var use_player_1 = require_use_player();
    var calculate_player_size_1 = require_calculate_player_size();
    var is_node_1 = require_is_node();
    var use_click_prevention_on_double_click_1 = require_use_click_prevention_on_double_click();
    var use_element_size_1 = require_use_element_size();
    var reactVersion = react_1.default.version.split(".")[0];
    if (reactVersion === "0") {
      throw new Error(`Version ${reactVersion} of "react" is not supported by Remotion`);
    }
    var doesReactVersionSupportSuspense = parseInt(reactVersion, 10) >= 18;
    var PlayerUI = ({ controls, style, loop, autoPlay, allowFullscreen, inputProps, clickToPlay, showVolumeControls, mediaVolume, mediaMuted, doubleClickToFullscreen, setMediaMuted, setMediaVolume, spaceKeyToPlayOrPause, errorFallback, playbackRate, renderLoading, className, moveToBeginningWhenEnded }, ref) => {
      var _a, _b, _c;
      const config = remotion_1.Internals.useUnsafeVideoConfig();
      const video = remotion_1.Internals.useVideo();
      const container = (0, react_1.useRef)(null);
      const hovered = (0, use_hover_state_1.useHoverState)(container);
      const canvasSize = (0, use_element_size_1.useElementSize)(container, {
        triggerOnWindowResize: false,
        shouldApplyCssTransforms: false
      });
      const [hasPausedToResume, setHasPausedToResume] = (0, react_1.useState)(false);
      const [shouldAutoplay, setShouldAutoPlay] = (0, react_1.useState)(autoPlay);
      const [isFullscreen, setIsFullscreen] = (0, react_1.useState)(() => false);
      (0, use_playback_1.usePlayback)({
        loop,
        playbackRate,
        moveToBeginningWhenEnded,
        inFrame: null,
        outFrame: null
      });
      const player = (0, use_player_1.usePlayer)();
      (0, react_1.useEffect)(() => {
        if (hasPausedToResume && !player.playing) {
          setHasPausedToResume(false);
          player.play();
        }
      }, [hasPausedToResume, player]);
      (0, react_1.useEffect)(() => {
        const { current } = container;
        if (!current) {
          return;
        }
        const onFullscreenChange = () => {
          setIsFullscreen(document.fullscreenElement === current || document.webkitFullscreenElement === current);
        };
        document.addEventListener("fullscreenchange", onFullscreenChange);
        document.addEventListener("webkitfullscreenchange", onFullscreenChange);
        return () => {
          document.removeEventListener("fullscreenchange", onFullscreenChange);
          document.removeEventListener("webkitfullscreenchange", onFullscreenChange);
        };
      }, []);
      const toggle = (0, react_1.useCallback)((e) => {
        if (player.isPlaying()) {
          player.pause();
        } else {
          player.play(e);
        }
      }, [player]);
      const requestFullscreen = (0, react_1.useCallback)(() => {
        if (!allowFullscreen) {
          throw new Error("allowFullscreen is false");
        }
        const supportsFullScreen = document.fullscreenEnabled || document.webkitFullscreenEnabled;
        if (!supportsFullScreen) {
          throw new Error("Browser doesnt support fullscreen");
        }
        if (!container.current) {
          throw new Error("No player ref found");
        }
        if (container.current.webkitRequestFullScreen) {
          container.current.webkitRequestFullScreen();
        } else {
          container.current.requestFullscreen();
        }
      }, [allowFullscreen]);
      const exitFullscreen = (0, react_1.useCallback)(() => {
        if (document.webkitExitFullscreen) {
          document.webkitExitFullscreen();
        } else {
          document.exitFullscreen();
        }
      }, []);
      (0, react_1.useEffect)(() => {
        const { current } = container;
        if (!current) {
          return;
        }
        const fullscreenChange = () => {
          var _a2;
          const element = (_a2 = document.webkitFullscreenElement) !== null && _a2 !== void 0 ? _a2 : document.fullscreenElement;
          if (element && element === container.current) {
            player.emitter.dispatchFullscreenChangeUpdate({
              isFullscreen: true
            });
          } else {
            player.emitter.dispatchFullscreenChangeUpdate({
              isFullscreen: false
            });
          }
        };
        current.addEventListener("webkitfullscreenchange", fullscreenChange);
        current.addEventListener("fullscreenchange", fullscreenChange);
        return () => {
          current.removeEventListener("webkitfullscreenchange", fullscreenChange);
          current.removeEventListener("fullscreenchange", fullscreenChange);
        };
      }, [player.emitter]);
      const durationInFrames = (_a = config === null || config === void 0 ? void 0 : config.durationInFrames) !== null && _a !== void 0 ? _a : 1;
      (0, react_1.useImperativeHandle)(ref, () => {
        const methods = {
          play: player.play,
          pause: player.pause,
          toggle,
          getContainerNode: () => container.current,
          getCurrentFrame: player.getCurrentFrame,
          isPlaying: () => player.playing,
          seekTo: (f) => {
            const lastFrame = durationInFrames - 1;
            const frameToSeekTo = Math.max(0, Math.min(lastFrame, f));
            if (player.isPlaying()) {
              const pauseToResume = frameToSeekTo !== lastFrame || loop;
              setHasPausedToResume(pauseToResume);
              player.pause();
            }
            if (frameToSeekTo === lastFrame && !loop) {
              player.emitter.dispatchEnded();
            }
            player.seek(frameToSeekTo);
          },
          isFullscreen: () => isFullscreen,
          requestFullscreen,
          exitFullscreen,
          getVolume: () => {
            if (mediaMuted) {
              return 0;
            }
            return mediaVolume;
          },
          setVolume: (vol) => {
            if (typeof vol !== "number") {
              throw new TypeError(`setVolume() takes a number, got value of type ${typeof vol}`);
            }
            if (isNaN(vol)) {
              throw new TypeError(`setVolume() got a number that is NaN. Volume must be between 0 and 1.`);
            }
            if (vol < 0 || vol > 1) {
              throw new TypeError(`setVolume() got a number that is out of range. Must be between 0 and 1, got ${typeof vol}`);
            }
            setMediaVolume(vol);
          },
          isMuted: () => mediaMuted || mediaVolume === 0,
          mute: () => {
            setMediaMuted(true);
          },
          unmute: () => {
            setMediaMuted(false);
          }
        };
        return Object.assign(player.emitter, methods);
      }, [
        durationInFrames,
        exitFullscreen,
        isFullscreen,
        loop,
        mediaMuted,
        mediaVolume,
        player,
        requestFullscreen,
        setMediaMuted,
        setMediaVolume,
        toggle
      ]);
      const VideoComponent = video ? video.component : null;
      const outerStyle = (0, react_1.useMemo)(() => {
        if (!config) {
          return {};
        }
        return {
          position: "relative",
          overflow: "hidden",
          ...(0, calculate_player_size_1.calculatePlayerSize)({
            compositionHeight: config.height,
            compositionWidth: config.width,
            currentSize: canvasSize,
            height: style === null || style === void 0 ? void 0 : style.height,
            width: style === null || style === void 0 ? void 0 : style.width
          }),
          ...style
        };
      }, [canvasSize, config, style]);
      const layout = (0, react_1.useMemo)(() => {
        if (!config || !canvasSize) {
          return null;
        }
        return (0, calculate_scale_1.calculateScale)({
          canvasSize,
          compositionHeight: config.height,
          compositionWidth: config.width,
          previewSize: "auto"
        });
      }, [canvasSize, config]);
      const outer = (0, react_1.useMemo)(() => {
        if (!layout || !config) {
          return {};
        }
        const { centerX, centerY, scale } = layout;
        return {
          width: config.width * scale,
          height: config.height * scale,
          display: "flex",
          flexDirection: "column",
          position: "absolute",
          left: centerX,
          top: centerY,
          overflow: "hidden"
        };
      }, [config, layout]);
      const containerStyle = (0, react_1.useMemo)(() => {
        if (!config || !canvasSize) {
          return {};
        }
        const { scale, xCorrection, yCorrection } = (0, calculate_scale_1.calculateScale)({
          canvasSize,
          compositionHeight: config.height,
          compositionWidth: config.width,
          previewSize: "auto"
        });
        return {
          position: "absolute",
          width: config.width,
          height: config.height,
          display: "flex",
          transform: `scale(${scale})`,
          marginLeft: xCorrection,
          marginTop: yCorrection,
          overflow: "hidden"
        };
      }, [canvasSize, config]);
      const onError = (0, react_1.useCallback)((error) => {
        player.pause();
        player.emitter.dispatchError(error);
      }, [player]);
      const onFullscreenButtonClick = (0, react_1.useCallback)((e) => {
        e.stopPropagation();
        requestFullscreen();
      }, [requestFullscreen]);
      const onExitFullscreenButtonClick = (0, react_1.useCallback)((e) => {
        e.stopPropagation();
        exitFullscreen();
      }, [exitFullscreen]);
      const onSingleClick = (0, react_1.useCallback)((e) => {
        toggle(e);
      }, [toggle]);
      const onDoubleClick = (0, react_1.useCallback)(() => {
        if (isFullscreen) {
          exitFullscreen();
        } else {
          requestFullscreen();
        }
      }, [exitFullscreen, isFullscreen, requestFullscreen]);
      const [handleClick, handleDoubleClick] = (0, use_click_prevention_on_double_click_1.useClickPreventionOnDoubleClick)(onSingleClick, onDoubleClick, doubleClickToFullscreen);
      (0, react_1.useEffect)(() => {
        if (shouldAutoplay) {
          player.play();
          setShouldAutoPlay(false);
        }
      }, [shouldAutoplay, player]);
      if (!config) {
        return null;
      }
      const content = (0, jsx_runtime_1.jsxs)(jsx_runtime_1.Fragment, { children: [(0, jsx_runtime_1.jsx)("div", { style: outer, onClick: clickToPlay ? handleClick : void 0, onDoubleClick: doubleClickToFullscreen ? handleDoubleClick : void 0, children: (0, jsx_runtime_1.jsx)("div", { style: containerStyle, className: player_css_classname_1.PLAYER_CSS_CLASSNAME, children: VideoComponent ? (0, jsx_runtime_1.jsx)(error_boundary_1.ErrorBoundary, { onError, errorFallback, children: (0, jsx_runtime_1.jsx)(VideoComponent, { ...(_b = video === null || video === void 0 ? void 0 : video.defaultProps) !== null && _b !== void 0 ? _b : {}, ...(_c = inputProps) !== null && _c !== void 0 ? _c : {} }) }) : null }) }), controls ? (0, jsx_runtime_1.jsx)(PlayerControls_1.Controls, { fps: config.fps, durationInFrames: config.durationInFrames, hovered, player, onFullscreenButtonClick, isFullscreen, allowFullscreen, showVolumeControls, onExitFullscreenButtonClick, spaceKeyToPlayOrPause }) : null] });
      if (is_node_1.IS_NODE && !doesReactVersionSupportSuspense) {
        return (0, jsx_runtime_1.jsx)("div", { ref: container, style: outerStyle, className, children: content });
      }
      const loadingMarkup = renderLoading ? renderLoading({
        height: outerStyle.height,
        width: outerStyle.width
      }) : null;
      return (0, jsx_runtime_1.jsx)("div", { ref: container, style: outerStyle, className, children: (0, jsx_runtime_1.jsx)(react_1.Suspense, { fallback: loadingMarkup, children: content }) });
    };
    exports.default = (0, react_1.forwardRef)(PlayerUI);
  }
});

// node_modules/@remotion/player/dist/utils/validate-playbackrate.js
var require_validate_playbackrate = __commonJS({
  "node_modules/@remotion/player/dist/utils/validate-playbackrate.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validatePlaybackRate = void 0;
    var validatePlaybackRate = (playbackRate) => {
      if (playbackRate === void 0) {
        return;
      }
      if (playbackRate > 4) {
        throw new Error(`The highest possible playback rate is 4. You passed: ${playbackRate}`);
      }
      if (playbackRate < -4) {
        throw new Error(`The lowest possible playback rate is -4. You passed: ${playbackRate}`);
      }
      if (playbackRate === 0) {
        throw new Error(`A playback rate of 0 is not supported.`);
      }
    };
    exports.validatePlaybackRate = validatePlaybackRate;
  }
});

// node_modules/@remotion/player/dist/volume-persistance.js
var require_volume_persistance = __commonJS({
  "node_modules/@remotion/player/dist/volume-persistance.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getPreferredVolume = exports.persistVolume = void 0;
    var VOLUME_PERSISTANCE_KEY = "remotion.volumePreference";
    var persistVolume = (volume) => {
      if (typeof window === "undefined") {
        return;
      }
      window.localStorage.setItem(VOLUME_PERSISTANCE_KEY, String(volume));
    };
    exports.persistVolume = persistVolume;
    var getPreferredVolume = () => {
      if (typeof window === "undefined") {
        return 1;
      }
      const val = window.localStorage.getItem(VOLUME_PERSISTANCE_KEY);
      return val ? Number(val) : 1;
    };
    exports.getPreferredVolume = getPreferredVolume;
  }
});

// node_modules/@remotion/player/dist/Player.js
var require_Player = __commonJS({
  "node_modules/@remotion/player/dist/Player.js"(exports) {
    "use strict";
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Player = exports.PlayerFn = exports.componentOrNullIfLazy = void 0;
    var jsx_runtime_1 = require_jsx_runtime();
    var react_1 = require_react();
    var remotion_1 = require_dist();
    var emitter_context_1 = require_emitter_context();
    var event_emitter_1 = require_event_emitter();
    var player_css_classname_1 = require_player_css_classname();
    var PlayerUI_1 = __importDefault(require_PlayerUI());
    var validate_playbackrate_1 = require_validate_playbackrate();
    var volume_persistance_1 = require_volume_persistance();
    remotion_1.Internals.CSSUtils.injectCSS(remotion_1.Internals.CSSUtils.makeDefaultCSS(`.${player_css_classname_1.PLAYER_CSS_CLASSNAME}`, "#fff"));
    var componentOrNullIfLazy = (props) => {
      if ("component" in props) {
        return props.component;
      }
      return null;
    };
    exports.componentOrNullIfLazy = componentOrNullIfLazy;
    var PlayerFn = ({ durationInFrames, compositionHeight, compositionWidth, fps, inputProps, style, controls = false, loop = false, autoPlay = false, showVolumeControls = true, allowFullscreen = true, clickToPlay, doubleClickToFullscreen = false, spaceKeyToPlayOrPause = true, moveToBeginningWhenEnded = true, numberOfSharedAudioTags = 5, errorFallback = () => "⚠️", playbackRate = 1, renderLoading, className, ...componentProps }, ref) => {
      if (typeof window !== "undefined") {
        (0, react_1.useLayoutEffect)(() => {
          window.remotion_isPlayer = true;
        }, []);
      }
      if (componentProps.defaultProps !== void 0) {
        throw new Error("The <Player /> component does not accept `defaultProps`, but some were passed. Use `inputProps` instead.");
      }
      const componentForValidation = (0, exports.componentOrNullIfLazy)(componentProps);
      if ((componentForValidation === null || componentForValidation === void 0 ? void 0 : componentForValidation.type) === remotion_1.Composition) {
        throw new TypeError(`'component' should not be an instance of <Composition/>. Pass the React component directly, and set the duration, fps and dimensions as separate props. See https://www.remotion.dev/docs/player/examples for an example.`);
      }
      if (componentForValidation === remotion_1.Composition) {
        throw new TypeError(`'component' must not be the 'Composition' component. Pass your own React component directly, and set the duration, fps and dimensions as separate props. See https://www.remotion.dev/docs/player/examples for an example.`);
      }
      const component = remotion_1.Internals.useLazyComponent(componentProps);
      const [frame, setFrame] = (0, react_1.useState)(0);
      const [playing, setPlaying] = (0, react_1.useState)(false);
      const [rootId] = (0, react_1.useState)("player-comp");
      const [emitter] = (0, react_1.useState)(() => new event_emitter_1.PlayerEmitter());
      const rootRef = (0, react_1.useRef)(null);
      const [mediaMuted, setMediaMuted] = (0, react_1.useState)(false);
      const [mediaVolume, setMediaVolume] = (0, react_1.useState)((0, volume_persistance_1.getPreferredVolume)());
      const audioAndVideoTags = (0, react_1.useRef)([]);
      const imperativePlaying = (0, react_1.useRef)(false);
      if (typeof compositionHeight !== "number") {
        throw new TypeError(`'compositionHeight' must be a number but got '${typeof compositionHeight}' instead`);
      }
      if (typeof compositionWidth !== "number") {
        throw new TypeError(`'compositionWidth' must be a number but got '${typeof compositionWidth}' instead`);
      }
      remotion_1.Internals.validateDimension(compositionHeight, "compositionHeight", "of the <Player /> component");
      remotion_1.Internals.validateDimension(compositionWidth, "compositionWidth", "of the <Player /> component");
      remotion_1.Internals.validateDurationInFrames(durationInFrames, "of the <Player/> component");
      remotion_1.Internals.validateFps(fps, "as a prop of the <Player/> component", false);
      if (typeof controls !== "boolean" && typeof controls !== "undefined") {
        throw new TypeError(`'controls' must be a boolean or undefined but got '${typeof controls}' instead`);
      }
      if (typeof autoPlay !== "boolean" && typeof autoPlay !== "undefined") {
        throw new TypeError(`'autoPlay' must be a boolean or undefined but got '${typeof autoPlay}' instead`);
      }
      if (typeof loop !== "boolean" && typeof loop !== "undefined") {
        throw new TypeError(`'loop' must be a boolean or undefined but got '${typeof loop}' instead`);
      }
      if (typeof doubleClickToFullscreen !== "boolean" && typeof doubleClickToFullscreen !== "undefined") {
        throw new TypeError(`'doubleClickToFullscreen' must be a boolean or undefined but got '${typeof doubleClickToFullscreen}' instead`);
      }
      if (typeof showVolumeControls !== "boolean" && typeof showVolumeControls !== "undefined") {
        throw new TypeError(`'showVolumeControls' must be a boolean or undefined but got '${typeof showVolumeControls}' instead`);
      }
      if (typeof allowFullscreen !== "boolean" && typeof allowFullscreen !== "undefined") {
        throw new TypeError(`'allowFullscreen' must be a boolean or undefined but got '${typeof allowFullscreen}' instead`);
      }
      if (typeof clickToPlay !== "boolean" && typeof clickToPlay !== "undefined") {
        throw new TypeError(`'clickToPlay' must be a boolean or undefined but got '${typeof clickToPlay}' instead`);
      }
      if (typeof spaceKeyToPlayOrPause !== "boolean" && typeof spaceKeyToPlayOrPause !== "undefined") {
        throw new TypeError(`'spaceKeyToPlayOrPause' must be a boolean or undefined but got '${typeof spaceKeyToPlayOrPause}' instead`);
      }
      if (typeof numberOfSharedAudioTags !== "number" || numberOfSharedAudioTags % 1 !== 0 || !Number.isFinite(numberOfSharedAudioTags) || Number.isNaN(numberOfSharedAudioTags) || numberOfSharedAudioTags < 0) {
        throw new TypeError(`'numberOfSharedAudioTags' must be an integer but got '${numberOfSharedAudioTags}' instead`);
      }
      (0, validate_playbackrate_1.validatePlaybackRate)(playbackRate);
      (0, react_1.useEffect)(() => {
        emitter.dispatchRatechange(playbackRate);
      }, [emitter, playbackRate]);
      const setMediaVolumeAndPersist = (0, react_1.useCallback)((vol) => {
        setMediaVolume(vol);
        (0, volume_persistance_1.persistVolume)(vol);
      }, []);
      (0, react_1.useImperativeHandle)(ref, () => rootRef.current);
      const timelineContextValue = (0, react_1.useMemo)(() => {
        return {
          frame,
          playing,
          rootId,
          shouldRegisterSequences: false,
          playbackRate,
          imperativePlaying,
          setPlaybackRate: () => {
            throw new Error("playback rate");
          },
          audioAndVideoTags
        };
      }, [frame, playbackRate, playing, rootId]);
      const setTimelineContextValue = (0, react_1.useMemo)(() => {
        return {
          setFrame,
          setPlaying
        };
      }, [setFrame]);
      const mediaVolumeContextValue = (0, react_1.useMemo)(() => {
        return {
          mediaMuted,
          mediaVolume
        };
      }, [mediaMuted, mediaVolume]);
      const setMediaVolumeContextValue = (0, react_1.useMemo)(() => {
        return {
          setMediaMuted,
          setMediaVolume: setMediaVolumeAndPersist
        };
      }, [setMediaVolumeAndPersist]);
      const compositionManagerContext = (0, react_1.useMemo)(() => {
        return {
          compositions: [
            {
              component,
              durationInFrames,
              height: compositionHeight,
              width: compositionWidth,
              fps,
              id: "player-comp",
              props: inputProps,
              nonce: 777,
              scale: 1,
              folderName: null,
              defaultProps: void 0,
              parentFolderName: null
            }
          ],
          folders: [],
          registerFolder: () => void 0,
          unregisterFolder: () => void 0,
          currentComposition: "player-comp",
          registerComposition: () => void 0,
          registerSequence: () => void 0,
          sequences: [],
          setCurrentComposition: () => void 0,
          unregisterComposition: () => void 0,
          unregisterSequence: () => void 0,
          registerAsset: () => void 0,
          unregisterAsset: () => void 0,
          assets: []
        };
      }, [
        component,
        durationInFrames,
        compositionHeight,
        compositionWidth,
        fps,
        inputProps
      ]);
      const passedInputProps = (0, react_1.useMemo)(() => {
        return inputProps !== null && inputProps !== void 0 ? inputProps : {};
      }, [inputProps]);
      return (0, jsx_runtime_1.jsx)(remotion_1.Internals.CanUseRemotionHooksProvider, { children: (0, jsx_runtime_1.jsx)(remotion_1.Internals.Timeline.TimelineContext.Provider, { value: timelineContextValue, children: (0, jsx_runtime_1.jsx)(remotion_1.Internals.Timeline.SetTimelineContext.Provider, { value: setTimelineContextValue, children: (0, jsx_runtime_1.jsx)(remotion_1.Internals.CompositionManager.Provider, { value: compositionManagerContext, children: (0, jsx_runtime_1.jsx)(remotion_1.Internals.MediaVolumeContext.Provider, { value: mediaVolumeContextValue, children: (0, jsx_runtime_1.jsx)(remotion_1.Internals.SetMediaVolumeContext.Provider, { value: setMediaVolumeContextValue, children: (0, jsx_runtime_1.jsx)(remotion_1.Internals.SharedAudioContextProvider, { numberOfAudioTags: numberOfSharedAudioTags, children: (0, jsx_runtime_1.jsx)(emitter_context_1.PlayerEventEmitterContext.Provider, { value: emitter, children: (0, jsx_runtime_1.jsx)(PlayerUI_1.default, { ref: rootRef, renderLoading, autoPlay: Boolean(autoPlay), loop: Boolean(loop), controls: Boolean(controls), errorFallback, style, inputProps: passedInputProps, allowFullscreen: Boolean(allowFullscreen), moveToBeginningWhenEnded: Boolean(moveToBeginningWhenEnded), clickToPlay: typeof clickToPlay === "boolean" ? clickToPlay : Boolean(controls), showVolumeControls: Boolean(showVolumeControls), setMediaVolume: setMediaVolumeAndPersist, mediaVolume, mediaMuted, doubleClickToFullscreen: Boolean(doubleClickToFullscreen), setMediaMuted, spaceKeyToPlayOrPause: Boolean(spaceKeyToPlayOrPause), playbackRate, className: className !== null && className !== void 0 ? className : void 0 }) }) }) }) }) }) }) }) });
    };
    exports.PlayerFn = PlayerFn;
    exports.Player = (0, react_1.forwardRef)(exports.PlayerFn);
  }
});

// node_modules/@remotion/player/dist/index.js
var require_dist2 = __commonJS({
  "node_modules/@remotion/player/dist/index.js"(exports) {
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PlayerInternals = exports.Player = void 0;
    var calculate_scale_1 = require_calculate_scale();
    var emitter_context_1 = require_emitter_context();
    var event_emitter_1 = require_event_emitter();
    var use_hover_state_1 = require_use_hover_state();
    var use_playback_1 = require_use_playback();
    var use_player_1 = require_use_player();
    var use_element_size_1 = require_use_element_size();
    var Player_1 = require_Player();
    Object.defineProperty(exports, "Player", { enumerable: true, get: function() {
      return Player_1.Player;
    } });
    exports.PlayerInternals = {
      PlayerEventEmitterContext: emitter_context_1.PlayerEventEmitterContext,
      PlayerEmitter: event_emitter_1.PlayerEmitter,
      usePlayer: use_player_1.usePlayer,
      usePlayback: use_playback_1.usePlayback,
      useElementSize: use_element_size_1.useElementSize,
      calculateScale: calculate_scale_1.calculateScale,
      useHoverState: use_hover_state_1.useHoverState,
      updateAllElementsSizes: use_element_size_1.updateAllElementsSizes
    };
  }
});
export default require_dist2();
//# sourceMappingURL=@remotion_player.js.map
