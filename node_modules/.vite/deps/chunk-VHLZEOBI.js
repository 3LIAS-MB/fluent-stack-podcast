import {
  continueRender,
  delayRender,
  init_esm
} from "./chunk-YHF3ULGN.js";

// node_modules/@remotion/google-fonts/dist/esm/base.mjs
init_esm();
var loadedFonts = {};
var loadFonts = (meta, style, options) => {
  var _a, _b, _c, _d;
  const styles = style ? [style] : Object.keys(meta.fonts);
  for (const style2 of styles) {
    if (typeof FontFace === "undefined") {
      continue;
    }
    if (!meta.fonts[style2]) {
      throw new Error(`The font ${meta.fontFamily} does not have a style ${style2}`);
    }
    const weights = (_a = options === null || options === void 0 ? void 0 : options.weights) !== null && _a !== void 0 ? _a : Object.keys(meta.fonts[style2]);
    for (const weight of weights) {
      if (!meta.fonts[style2][weight]) {
        throw new Error(`The font ${meta.fontFamily} does not have a weight ${weight} in style ${style2}`);
      }
      const subsets = (_b = options === null || options === void 0 ? void 0 : options.subsets) !== null && _b !== void 0 ? _b : Object.keys(meta.fonts[style2][weight]);
      for (const subset of subsets) {
        let font = (_d = (_c = meta.fonts[style2]) === null || _c === void 0 ? void 0 : _c[weight]) === null || _d === void 0 ? void 0 : _d[subset];
        if (!font) {
          throw new Error(`weight: ${weight} subset: ${subset} is not available for '${meta.fontFamily}'`);
        }
        let fontKey = `${meta.fontFamily}-${style2}-${weight}-${subset}`;
        if (loadedFonts[fontKey]) {
          continue;
        }
        loadedFonts[fontKey] = true;
        const handle = delayRender(`Fetching ${meta.fontFamily} font ${JSON.stringify({
          style: style2,
          weight,
          subset
        })}`);
        const fontFace = new FontFace(meta.fontFamily, `url(${font}) format('woff2')`, {
          weight,
          style: style2,
          unicodeRange: meta.unicodeRanges[subset]
        });
        fontFace.load().then(() => {
          var _a2;
          ((_a2 = options === null || options === void 0 ? void 0 : options.document) !== null && _a2 !== void 0 ? _a2 : document).fonts.add(fontFace);
          continueRender(handle);
        }).catch((err) => {
          loadedFonts[fontKey] = false;
          throw err;
        });
      }
    }
  }
  return {
    fontFamily: meta.fontFamily,
    fonts: meta.fonts,
    unicodeRanges: meta.unicodeRanges
  };
};

export {
  loadFonts
};
//# sourceMappingURL=chunk-VHLZEOBI.js.map
